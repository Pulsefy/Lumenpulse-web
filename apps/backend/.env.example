# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=starkpulse

# Application Configuration
NODE_ENV=development
PORT=3000

# Database Synchronization (only for development - use migrations in production)
# Set to true only in development to auto-sync schema (not recommended)
DB_SYNC=false
